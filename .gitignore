# XcodeGen 產生，不進 git（見 AGENTS.md）
*.xcodeproj
# 本機簽署設定（team ID），不進 git
app/Local.xcconfig

.DS_Store

.env
.env.*
__pycache__/
.venv/
*.p12
*.mobileprovision

# 驗證 skill 的本機證據與 build 輸出（證據發布到 godmosword/project-kid-ai-evidence）
.verify/
app/build/
